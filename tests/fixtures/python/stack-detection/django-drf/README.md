# Django + DRF Stack Detection Fixture

- **Expected Language:** Python
- **Expected Framework:** Django, Django REST Framework (DRF)
- **Expected Data Layer:** Django ORM
- **Expected Confidence:** Confirmed
- **Evidence:** `pyproject.toml` containing `django` and `djangorestframework` dependencies.
